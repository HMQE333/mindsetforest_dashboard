/**
 * Canonical public origin for share links.
 * Always used when generating shareable URLs so links work for anyone,
 * regardless of whether the owner is currently in preview or production.
 */
export const PUBLIC_SHARE_ORIGIN = "https://hmqe.org";